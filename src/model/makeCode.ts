import { Shape } from "../store/shapes/types"
import ArrowMap from "../interfaces/arrowMap"

export const generateCode = (sortedList: Shape[], arrows: ArrowMap) => {
	for(let i = 0; i < sortedList.length; i++) {
		//console.log()
	}
}